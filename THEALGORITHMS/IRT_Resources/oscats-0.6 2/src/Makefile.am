## Makefile.am -- Process this file with automake to produce Makefile.in

DIST_SUBDIRS = liboscats # liboscats-mmle liboscats-sim sim
SUBDIRS = liboscats # $(MAYBE_SIMLIB) $(MAYBE_MMLELIB) $(MAYBE_SIM)
