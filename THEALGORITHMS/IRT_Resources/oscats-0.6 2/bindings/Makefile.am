## Makefile.am -- Process this file with automake to produce Makefile.in

DIST_SUBDIRS = python perl php java
SUBDIRS = $(MAYBE_PYBIND) $(MAYBE_PLBIND) $(MAYBE_PHPBIND) $(MAYBE_JAVABIND)

EXTRA_DIST = oscats-common.defs
